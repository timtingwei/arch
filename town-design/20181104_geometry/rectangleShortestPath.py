#!/usr/bin/env python
#-*- coding: utf-8 -*-

import geometry

# 已经判断好两个矩形各个角点的可见性
def findShortestPath(rec1, rec2, p):
    # 计算得到两个矩形之间的最短路径

    # 判断起点和终点之间的可见性

    # 如果可见, 得到可见点之间的路径

    # 如果不可见,

    # 得到起点和终点到各自矩形到可见端点的路径

    # 将中间路径(可见点之间的路径), 起点到端点的路径累加, 得到所有路径

    # 比较路径, 选择距离最短的路径
    return
